// This adds a plugin for the java language
apply plugin: 'java'

// This tells gradle where to look for libraries that your code is using
// (for example the JUnit test framework
repositories {
    jcenter()
}

// This defines any other code that your code depends on
dependencies {
    testCompile 'junit:junit:4.12'
}
